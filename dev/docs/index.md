# Index

Preview harness for the shared config. Each line below exercises one thing
`base.yml` turns on, so a broken build is visible on this page.

- Emoji (`pymdownx.emoji` → twemoji): :smile: :rocket:
- Math (`arithmatex` + KaTeX): $E = mc^2$
- Admonition:

!!! note

    Rendered by `admonition`.

See [templates](templates_base/templates.md) and [mermaid](templates_base/mermaid.md)
for the rest.
